from bot import AutoFishingBot


if __name__ == "__main__":
    AutoFishingBot().run_forever()
    # find_items_can_buy = AutoFishingBot().find_items_can_buy()
    # print(f"Items that can be bought: {find_items_can_buy}")
    # choose_bait_item = AutoFishingBot().choose_bait_item(find_items_can_buy)
    # print(f"Chosen bait item: {choose_bait_item}")